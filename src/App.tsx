import React, { FC, Fragment } from "react";
import { Background } from "./components/Background";

const App: FC = () => {
	return (
		<Fragment>
			<Background />
		</Fragment>
	);
};

export default App;
